/**
 * Martin's public profile links — the proof surfaces (C2). One source for the
 * About panel and the site footer; the contact CTA itself stays email-only in
 * `chapters.ts` (CLAUDE.md). LinkedIn is kept quiet (footer/About only) until
 * Martin refreshes the profile (martin-facts).
 */
export const PROFILE = {
  github: { label: 'GitHub', href: 'https://github.com/Martin8O' },
  linkedin: {
    label: 'LinkedIn',
    href: 'https://www.linkedin.com/in/martin-svoboda-43a45429b/',
  },
} as const
