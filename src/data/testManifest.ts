/**
 * GENERATED FILE — do not edit. Rebuild with `npm run gen:tests`
 * (scripts/gen-test-manifest.mjs bakes `vitest list` into this static
 * list; the TestsPopup lazy chunk is its only importer, so the manifest
 * costs the initial bundle nothing).
 */

export type TestFileEntry = {
  /** Repo-relative test file. */
  file: string
  /** Collected case names, "suite > test", in file order. */
  tests: string[]
}

export const TEST_COUNT = 359

export const TEST_MANIFEST: TestFileEntry[] = [
  {
    file: "src/canvas/sceneTimeline.test.ts",
    tests: [
      "buildRuns > groups contiguous chapters of the same theme into one run",
      "buildRuns > splits sky chapters per sub-mood (each mood = its own scene)",
      "buildRuns > covers every chapter exactly once (real data)",
      "runLocalT > first run: window starts at pos 0 and ends half a chapter past the run",
      "runLocalT > tRaw keeps flowing past the window edges (ambient motion never freezes)",
      "runLocalT > middle run: window spans ±0.5 chapters around the run",
      "runLocalT > last run: window ends exactly at the story end",
      "runLocalT > clamps outside the window",
      "resolveSceneFrame > mid-chapter: one base scene at full alpha, no incoming",
      "resolveSceneFrame > no transition inside a run (origin chapter 0 → 1 shares the scene)",
      "resolveSceneFrame > cross-fade opens after the climb fade-in of the run-boundary chapter",
      "resolveSceneFrame > blend grows monotonically through the zone and reaches 1",
      "resolveSceneFrame > after the fade the next scene becomes the base (no dead layer below)",
      "resolveSceneFrame > the sunset waits for the flares + the head-on pass: enterFade override",
      "resolveSceneFrame > story ends: last scene stays, clamped",
      "resolveSceneFrame > empty story yields null",
    ],
  },
  {
    file: "src/canvas/scenes/bitcoinMath.test.ts",
    tests: [
      "terrainHeight > is deterministic and stays in a sane range",
      "terrainHeight > flattens a clearing around the pad and raises a far wall",
      "project > puts the orbit centre on the vertical axis and the ground below it",
      "project > shrinks with depth and lifts far points toward the horizon",
      "project > raises higher world points up the screen",
      "project > yaw is horizontally symmetric",
      "camFromPointer > rests at the base pitch with no pointer and clamps extremes",
      "network layout > places the full population deterministically",
      "network layout > fits the denser rev12 population with the smaller separation",
      "network layout > per-node k keeps peripheral peers sparsely linked",
      "network layout > keeps peers apart and off the clearing",
      "network layout > links every peer, with no self-loops or duplicates",
      "network layout > nominates the coin peers nearest the pad",
      "story choreography > the impact wave is absent before 87 %, then sweeps the whole valley",
      "story choreography > peers stay dark until the front reaches them, then hold",
      "story choreography > waveBand honours a custom half-width",
      "story choreography > terrain trace-in runs hidden → drawn, outward, inside its window",
      "rhythm > heartbeat is full at time 0 (reduced-motion frame) and bounded",
      "rhythm > heartbeat repeats with its period",
      "rhythm > no block wave is in flight at time 0; radius grows monotonically",
      "rhythm > waveBand excites only a narrow travelling band",
      "rhythm > pulsePhase stays in [0,1) at any time",
      "cursorBoost > peaks at the cursor, fades smoothly, dies beyond reach",
    ],
  },
  {
    file: "src/canvas/scenes/calmMath.test.ts",
    tests: [
      "calmLight > opens in the dark and reaches full light before the run ends",
      "calmLight > holds the warm blush back until the light is well up",
      "calmLight > both phases rise monotonically",
      "breath > rests exhaled at time 0 — the reduced-motion frame is complete",
      "breath > peaks fully inhaled mid-cycle and repeats each period",
      "breath > stays within 0..1",
      "stoneReveal > keeps the water empty before the first step",
      "stoneReveal > surfaces the stones strictly one after another — krok za krokem",
      "stoneReveal > has every stone settled before the scene hands over",
      "rippleTrainAt > is silent before the drop and after the disturbance has passed",
      "rippleTrainAt > launches the leading ring first, the trailing rings on a delay",
      "rippleTrainAt > keeps the train ordered — every trailing ring tighter than its leader",
      "rippleTrainAt > spends its energy as it spreads — amplitude falls off with the run-out",
      "stonePath > walks from the meditator island up-left toward the tree island",
      "stonePath > keeps the slant stride constant relative to the stones it separates",
      "stonePath > stops short of the tree island — the path arrives, never overshoots",
      "buildTree > is deterministic — the same seed grows the same tree",
      "buildTree > starts from a single trunk and branches richly",
      "buildTree > keeps every branch inside a sane canopy and always climbing",
      "buildTree > tapers: every branch is thinner at the tip, twigs thinner than boughs",
      "buildTree > puts blossoms only on the outer twigs",
      "buildTree > stands pre-grown through the base depths — the old tree is simply there",
      "buildTree > extends the fine outgrowth in staggered waves, complete at full growth",
      "treeGrow > waits for the healing to begin, fully grown before the end",
      "bloom > flowers only once the fine growth is under way, complete before the hand-off",
    ],
  },
  {
    file: "src/canvas/scenes/contactMath.test.ts",
    tests: [
      "fract > wraps into [0,1) for negatives too",
      "breath > stays in [0,1] across many cycles",
      "breath > is periodic in breathPeriod",
      "breath > is near-full at time 0 — the reduced-motion static frame",
      "breath > reaches both a full and an empty moment each cycle",
      "breath > inhales faster than it exhales (asymmetric cycle)",
      "breathWave > is TAU-periodic in angle",
      "breathWave > different angles breathe out of phase",
      "bloom > reach grows monotonically from the seed to full",
      "bloom > presence arrives before the reach finishes growing",
      "bloom > is quiet before the bloom window (the dev world still owns the frame)",
      "petalReach > is deterministic, clamped to [0.3, 1] and TAU-periodic",
      "petalReach > has real lobes and valleys — never a clean circle",
      "filamentAngle > fans the full circle with jitter but no collisions",
      "filamentGlow > keeps the centre lit but modest, tips still carrying light",
      "filamentGlow > peaks in a band around halfway out the strand",
      "spore > is deterministic and drifts outward through its life",
      "spore > fades at both ends of the drift (born + dies invisible)",
      "dust > is deterministic with all fields in range",
      "dust > drifts toward the camera and recycles after one period",
      "storyMix > anchors segment 0 at the top of the wheel and is TAU-periodic",
      "storyMix > walks all segments exactly once around the ring, t in [0,1)",
      "pulse > is quiet at time 0 (static frame holds no half-born ring)",
      "pulse > runs a fading front once per period",
      "pulse > the front expands while it lives",
    ],
  },
  {
    file: "src/canvas/scenes/devMath.test.ts",
    tests: [
      "windowSpawn > is 0 before birth and 1 at the very end for every window",
      "windowSpawn > rises monotonically through a window flight",
      "windowSpawn > follows Martin's staircase: 1/5 · then 2/5 + 1/4 · then 3/5 + 2/4 + 1/3 …",
      "windowSpawn > lands all five together at 100 %, none earlier",
      "windowSpawn > staggers strictly among the born windows",
      "spawnFlash > is zero outside the birth burst and peaks mid-burst",
      "easeOutBack > pins the endpoints and overshoots between",
      "easeOutBack > clamps its input",
      "floorPoint > collapses to the vanishing point at the horizon",
      "floorPoint > reaches the near edge at full depth",
      "floorPoint > descends monotonically from horizon to near edge",
      "floorPoint > spreads columns wider as they near the viewer",
      "corePulse > stays within 0..1 and glows at time 0",
      "corePulse > repeats on its period",
      "streamPhase > stays in [0,1) and is deterministic",
      "streamPhase > advances with time",
      "lensWarp > is the identity far from the core, at the core, and with r <= 0",
      "lensWarp > pulls nearby points toward the core (space pinches)",
      "lensWarp > pulls proportionally harder the closer the point is",
      "lensWarp > swirl rotates without changing the distance when k = 0",
      "lensWarp > never flips a point past the core for k < 1",
      "windowLayout > returns one slot per window, deterministically",
      "windowLayout > keeps every window clear of the central card box",
      "windowLayout > pushes the flanks outward on wider viewports",
      "windowLayout > stays on-screen",
      "windowLayout > drops RL Lab + BrainQuest to the bottom corners in portrait",
    ],
  },
  {
    file: "src/canvas/scenes/offerMath.test.ts",
    tests: [
      "route geometry > normalizes cumulative arc-lengths from 0 to 1, strictly increasing",
      "route geometry > interpolates the head exactly onto the nodes",
      "route geometry > flows START(1) upper-left → 2 → 3 → DEST(4) lower-right, rightward",
      "route geometry > keeps every node on the plate, distinct from its neighbours",
      "route geometry > reports the leg under the head",
      "choreography > chart materializes before the plotting starts",
      "choreography > plots monotonically and finishes before the scene hands over",
      "choreography > reveals waypoints in route order, right behind the pencil",
      "panel windows (derived from the route) > panel N rises exactly at its own corner waypoint N",
      "panel windows (derived from the route) > windows are ordered and valid",
      "panel windows (derived from the route) > mobile panels yield before the next waypoint lands",
      "panel windows (derived from the route) > clears the frame before the contact galaxy blooms (~ pos +0.72)",
      "panel windows (derived from the route) > panelWindows returns the matching pair",
      "ambient traffic > freezes at the offset position under reduced motion (time 0)",
      "ambient traffic > wraps around the path after one full traversal",
      "ambient traffic > faster contacts cover more ground per second",
      "ambient traffic > heading follows the current leg (turns at junctions)",
      "ambient traffic > gives every contact a fictional OK- reg, 250–400 kt speed, squawk and TWO turns",
      "ambient traffic > exposes the leg delta so callers can build an aspect-correct heading",
    ],
  },
  {
    file: "src/canvas/scenes/sky/skyMath.test.ts",
    tests: [
      "graduationAt > starts on the ultralight with no pulse",
      "graduationAt > steps up to the L-39C in ladder order — the L-159 belongs to cruise",
      "graduationAt > never steps down as t grows",
      "graduationAt > pulses at each swap and decays shortly after",
      "graduationAt > completes the ladder before the cloud-punch begins",
      "cloudPunch > is clear below and above the layer, white inside",
      "cloudPunch > cuts out of the white-out hard — no long dissolve",
      "cloudPunch > swaps the world entirely inside the white-out window",
      "cloudPunch > approach rises monotonically toward the deck",
      "helixPoint > keeps the two jets on opposite sides of the axis, same height",
      "helixPoint > stays on the unit cylinder and alternates near/far with the turns",
      "sunArc > is continuous — no jump anywhere along the section",
      "sunArc > never stalls mid-section: x strictly grows between the endpoints",
      "sunArc > starts upper-left, hands over at the seams, sets low right",
      "rollFrame > hits the traced quarter points of a full roll",
      "rollFrame > folds negative and >2π angles onto the same poses",
      "rollFrame > walks the ladder monotonically from level to knife-edge",
      "rollFrame > mirrors instead of popping at the quarter points",
      "silhouette data > ships one traced frame per bank step",
      "silhouette data > every silhouette is normalized: unit length, centred, detailed",
      "poseFold > walks side → belly → inverted → above → side over one full roll",
      "poseFold > keeps el continuous and flips only at the poles",
      "opposingDisplay > moves and turns continuously through every segment seam",
      "opposingDisplay > meets its mirror at the centre: head-on pass, loop foot, farewell pass",
      "opposingDisplay > finishes the first vykrut before the head-on pass",
      "opposingDisplay > half-rolls at 59 % and crosses the LOW second pass upright",
      "opposingDisplay > flies the loop wings-level, meeting height at the top",
      "opposingDisplay > half-rolls over the wingover top and rolls out by the dive",
      "opposingDisplay > climbs out steady 60–61 %, rolls 61–62.5 %, exits in normal attitude",
      "opposingDisplay > fires flares from 61 % to the exit and is clean off-screen by it",
      "opposingDisplay > stays inside the canvas width on a tall portrait screen",
      "l159 pose grid > covers the full el ladder × az fan, poses normalized to the length",
      "l159 pose grid > anchors the az-90 ladder on the roll axis — the nose tip line",
      "l159 pose grid > ships the gear-down rear view for the landing beat",
      "landingPov > orders the beats: sweep → black → reveal → touchdown → stop",
      "landingPov > hides the pass-over swap under FULL black, and only there",
      "landingPov > moves forward monotonically and touches down exactly on the threshold",
      "landingPov > keeps the wheels descending to the threshold and down after it",
      "landingPov > bites the brakes at the wheels and eases to the stop at the runway half",
      "landingPov > projects depth monotonically with the threshold as the unit",
    ],
  },
  {
    file: "src/data/testManifest.test.ts",
    tests: [
      "testManifest (generated — npm run gen:tests) > TEST_COUNT equals the baked list",
      "testManifest (generated — npm run gen:tests) > files are unique, repo-relative test modules with non-empty case names",
      "testManifest (generated — npm run gen:tests) > the proof-panel copy tells the same number (EN + CZ)",
    ],
  },
  {
    file: "src/scroll/scrollStore.test.ts",
    tests: [
      "scroll lock (counting gate) > replays the current state to a newly registered driver",
      "scroll lock (counting gate) > locks on first acquire, releases on last, edges only",
      "scroll lock (counting gate) > overlapping holders cannot steal each other’s lock",
      "scroll lock (counting gate) > an unbalanced release floors at zero and the next acquire still locks",
    ],
  },
  {
    file: "src/story/accent.test.ts",
    tests: [
      "mixHex > returns the endpoints at t=0 and t=1",
      "mixHex > mixes channel-wise at the midpoint",
      "mixHex > clamps t outside 0..1",
      "mixHex > pads single-digit channels",
      "accentAt > holds the pure theme accent away from boundaries",
      "accentAt > blends across the scene cross-fade exactly like the canvas",
      "accentAt > respects a per-chapter enterFade override",
    ],
  },
  {
    file: "src/three/bagramMath.test.ts",
    tests: [
      "ground model > projects the ground plane onto the 2D bands: horizon at 0.6, stands ~0.68, apron ~0.76",
      "ground model > groundY round-trips through the projection at any depth",
      "heliports > drift left with the pan at their band rate (ground-locked at their depth)",
      "heliports > gives each stand two symmetric touchdown marks that sit inside the slab",
      "heliports > spreads the two stands apart on a narrow portrait so they never collapse",
      "heliports > the rotary line sits RIGHT of the tower on its band (the blue zone)",
      "C-17 departure > lives from the fade-in roll to the exit, gone after",
      "C-17 departure > starts on the UPPER runway under the mountains, HIGH + clearly readable",
      "C-17 departure > rolls LEFT→RIGHT along the runway heading, accelerating, then rotates",
      "C-17 departure > climbs STRAIGHT ahead first (no bank), then banks RIGHT in the sweep",
      "C-17 departure > the right turn brings it TOWARD the viewer, CLIMBING to centre-top",
      "C-17 departure > never recedes, and ends CLOSE — larger than it started",
      "C-17 departure > climbs OUT the top edge, banked + grown (Martin R10: up, not down)",
      "C-17 departure > keeps speed continuous across liftoff and bank entry (no jerk)",
      "Apache pair arrival > enters HUGE over the top through the viewer, then rushes forward into the base",
      "Apache pair arrival > flies a fast fly-through, then the flare settles slow to touchdown",
      "Apache pair arrival > flares (nose-up pulse) and settles with a dying sink rate",
      "Apache pair arrival > lands each ship on its own stand MARK, wheel-ref on the deck (both aspects)",
      "Apache pair arrival > the wingman trails the lead in and never collides",
      "Mi-17 pair departure > both SIT on their own stand mark through the whole Apache overflight",
      "Mi-17 pair departure > lifts vertically (no horizontal run before the nose-over)",
      "Mi-17 pair departure > noses over, accelerates LEFT and away off the frame, wingman in trail",
      "Mi-17 pair departure > never conflicts with the Apache arrival (the two flows stay apart)",
      "F-16 holding pattern (time-driven ambient) > is a closed loop: one lap returns the ship to the same pose",
      "F-16 holding pattern (time-driven ambient) > the two ships hold opposite sides of the racetrack",
      "F-16 holding pattern (time-driven ambient) > freezes when time freezes (reduced-motion contract)",
      "F-16 holding pattern (time-driven ambient) > flies far, small and high, inside the frame on both aspects",
      "F-16 holding pattern (time-driven ambient) > banks into the turns (coordinated), wings level on the legs",
      "presence + ground shadows > composes the desert share exactly like the 2D slots",
      "presence + ground shadows > the shadow-catcher plane equation matches groundY at every depth",
      "presence + ground shadows > headingOf points the nose down the requested direction",
    ],
  },
  {
    file: "src/three/climbMath.test.ts",
    tests: [
      "snapshot timing (window-t of the climb run) > places snapshots at SEQ_START_T + (start + Σ steps) · SCROLL_TO_T",
      "snapshot timing (window-t of the climb run) > hands off types at the junctions: same point, touching spans",
      "snapshot timing (window-t of the climb run) > unlock spheres announce each type switch (the authored 0.17 % lead)",
      "snapshot timing (window-t of the climb run) > the flight tops out right at the section-ending white-out",
      "snapshot timing (window-t of the climb run) > preserves the authored scroll pacing through SCROLL_TO_T",
      "snapshot timing (window-t of the climb run) > the punch-out beats sit between the white-out and the window end",
      "stage projection (climbScreenAt) > maps the display plane edge-to-edge at the lab 3:2 aspect",
      "stage projection (climbScreenAt) > compresses deeper points toward the centre (perspective)",
      "stage projection (climbScreenAt) > climbXScale spans the width on desktop and pulls a portrait back in",
      "life alpha > is instant at the span edges (clean type handoffs)",
      "life alpha > holdBefore keeps the first aircraft parked through the fade-in",
      "life alpha > the L-39 lives to its last snap — the white-out does the swallowing",
      "monotone time warp (Fritsch–Carlson) > is exact at every knot (keyframe timing preserved)",
      "monotone time warp (Fritsch–Carlson) > is monotone non-decreasing across the whole span",
      "monotone time warp (Fritsch–Carlson) > clamps outside the span (parked at the path ends)",
      "monotone time warp (Fritsch–Carlson) > has C1-continuous slope at interior knots (no speed jumps)",
      "centripetal Catmull-Rom > passes through every control point",
      "centripetal Catmull-Rom > matches THREE.CatmullRomCurve3 centripetal on a known segment",
      "slerp > interpolates the shortest path and normalizes",
      "slerp > takes the short way around opposite-sign twins",
      "pose evaluation > parks at the first snap before the span and last after it",
      "pose evaluation > hits every snapshot pose exactly at its window-t knot",
      "pose evaluation > heroPosAt hands the flight across types continuously",
      "unlock sphere > is off outside its window",
      "unlock sphere > grows r0→r1 and dissolves as it grows",
      "name tags (the 2D graduation-tag envelope) > holds dim while the type flies, gone outside its life",
      "name tags (the 2D graduation-tag envelope) > pulses gold right after each unlock",
      "name tags (the 2D graduation-tag envelope) > the first rung has no pulse — just the dim ride (2D graduationAt)",
      "sky presence > composes exactly like the 2D paint order",
    ],
  },
  {
    file: "src/three/flightMath.test.ts",
    tests: [
      "buildFlightPath > is deterministic — same story, same flight",
      "buildFlightPath > covers the whole story on the stop grid",
      "buildFlightPath > travels each registered window by exactly its TRAVEL distance",
      "buildFlightPath > cruises between scenes",
      "flightPoseAt > always flies forward — z strictly decreases with scroll",
      "flightPoseAt > keeps the heading close to straight ahead (unit forward, fz ≤ −0.98)",
      "flightPoseAt > keeps the weave and bank gentle",
      "flightPoseAt > clamps outside the story",
      "flightPoseAt > is continuous — no jumps across stop boundaries",
      "flightAnchorAt > anchors at the window start, aimed down the window chord",
    ],
  },
  {
    file: "src/three/jetGeometry.test.ts",
    tests: [
      "bakeJet > bakes consistent non-indexed triangle buffers for both variants",
      "bakeJet > is deterministic",
      "bakeJet > produces finite geometry, unit normals, and 0..1 colors",
      "bakeJet > spans length 1 nose-to-tail and stays inside sane bounds",
      "bakeJet > is mirror-symmetric across the fuselage plane",
      "bakeJet > gives the two types distinct silhouettes (tanks vs rails, canopy)",
    ],
  },
  {
    file: "src/three/jetMath.test.ts",
    tests: [
      "flightEnvelope / flightProgress > is zero outside the window, full inside, eased at the edges",
      "flightEnvelope / flightProgress > progress maps the window onto 0..1, clamped",
      "story hard gates (the choreography table) > climb has no parametric flights — the GLB heroes own that window",
      "story hard gates (the choreography table) > sunset farewell is gone before the belly sweep blacks the screen",
      "story hard gates (the choreography table) > airshow passes finish before the 2D farewell flares (tRaw 0.995+)",
      "story hard gates (the choreography table) > desert has no flights — the empty sky is the story",
      "story hard gates (the choreography table) > every window is well-formed and fades fit inside it",
      "jetPoseAt > passes through every stop at its grid parameter",
      "jetPoseAt > keeps the forward unit-length and the path continuous",
      "jetPoseAt > clamps outside 0..1",
      "rollAt > hits keyframes exactly and lerps between them",
      "rollAt > clamps beyond the keyframe range and defaults to 0 without keys",
      "bakeRibbon > bakes a well-formed strip: pairs, ascending progress, unit tangents",
    ],
  },
  {
    file: "src/three/owned3d.test.ts",
    tests: [
      "paints2D > ships with an EMPTY owned set — no scene has flipped to 3D-owned",
      "paints2D > in 2d mode the 2D stage paints everything, whatever is owned",
      "paints2D > in 3d mode only owned themes are skipped",
      "paints2D > with the shipped set, 3d mode still paints everything",
      "paintsHero2D (the hero-level flip, E3b) > ships with the climb + cruise + desert flipped (cruise = the ballet corkscrew, desert = the Bagram flying actors)",
      "paintsHero2D (the hero-level flip, E3b) > keeps the 2D hero until the 3D scene reports READY",
      "paintsHero2D (the hero-level flip, E3b) > never touches 2d mode, non-flipped moods, or non-sky themes",
    ],
  },
  {
    file: "src/three/patrolMath.test.ts",
    tests: [
      "screen anchoring > screenOf inverts anchorPoint",
      "airshow head-on pass > lives only inside its window",
      "airshow head-on pass > approaches the crowd head-on: closing monotonically, near the centre",
      "airshow head-on pass > holds the echelon: wingman deeper by the longitudinal split",
      "airshow head-on pass > flies the simultaneous mirrored 3/4 vykrut",
      "airshow head-on pass > crosses on screen at a safe depth split — the near-miss illusion",
      "airshow head-on pass > streaks off through opposite screen edges",
      "airshow head-on pass > drives the smoke head along the rail",
      "sunset landing break > lives only inside its window (and dies before the hand-over veil)",
      "sunset landing break > punches in from BEHIND the observer and rushes out to the fix",
      "sunset landing break > HANGS close through the first scroll stop before the rush away",
      "sunset landing break > arrives over the field on the 40 %-height line at the break fix",
      "sunset landing break > leader breaks first; the wingman holds echelon one tick longer",
      "sunset landing break > the 180° comes out right of the tower, heading back at the camera",
      "sunset landing break > flies the echelon offset before the break",
      "sunset landing break > keeps every forward vector unit length",
    ],
  },
  {
    file: "src/three/scenes3d/balletMath.test.ts",
    tests: [
      "the beat windows (Martin directs in HUD %) > sit exactly on the directed HUD percents through the live weight map",
      "the beat windows (Martin directs in HUD %) > live inside the cruise run window, ordered, with COMAO riding the fade-out",
      "the beat windows (Martin directs in HUD %) > presence is BINARY: pops in at 21, holds the fight, pops out mid-29",
      "the beat windows (Martin directs in HUD %) > the maneuver clock is monotonic and flies the authored revolutions",
      "the beat windows (Martin directs in HUD %) > clouds sink across the fight and hold — never fully off frame",
      "the helix > flies the pair π apart — never colliding, breathing apart and back",
      "the helix > climbs — the centroid gains height monotonically",
      "the coordinated-turn pose (the physics — do not regress) > banks INTO the turn at every phase of a full revolution",
      "the coordinated-turn pose (the physics — do not regress) > keeps the bank graceful — peaks ~60–65°, never knife-edge",
      "the coordinated-turn pose (the physics — do not regress) > flies the path — unit forward, continuous over small steps",
      "the orbit camera (Martin: the pair must NEVER leave the frame) > fits the widest pass inside the horizontal frustum at any aspect",
      "the orbit camera (Martin: the pair must NEVER leave the frame) > tracks the climbing centroid (the clouds must sink for a reason)",
      "the orbit camera (Martin: the pair must NEVER leave the frame) > is a pure function of scroll — scrubbing back replays it exactly",
    ],
  },
  {
    file: "src/three/starfieldMath.test.ts",
    tests: [
      "genStars > bakes buffers of the right shape",
      "genStars > is deterministic — same seed, same sky",
      "genStars > keeps every star inside the view cone and depth band",
      "genStars > bakes finite, positive sizes and 0..1 colors/phases",
      "presence curves > origin: full night at the start, dead once the dawn brightens",
      "presence curves > origin: monotonically yields to daylight",
      "presence curves > contact: arrives ahead of the galaxy bloom (bloomT0 = 0.44) and holds",
      "presence curves > dispatches by theme; unregistered themes stay dark",
    ],
  },
  {
    file: "src/three/worldMode.test.ts",
    tests: [
      "parseWorldOverride > reads the two valid values",
      "parseWorldOverride > treats anything else as no override",
      "resolveWorldMode > defaults to 3d on a capable, motion-ok client",
      "resolveWorldMode > reduced motion wins over everything — even an explicit ?world=3d",
      "resolveWorldMode > no WebGL2 wins over an explicit ?world=3d",
      "resolveWorldMode > ?world=2d is a kill-switch on an otherwise capable client",
      "resolveWorldMode > the visitor toggle decides when no URL override is present",
      "resolveWorldMode > a weak client auto-falls back to 2D — unless the visitor chose 3D",
      "resolveWorldMode > the runtime FPS watchdog drops to 2D — but the visitor can still force 3D",
      "isWeakClient > reads little memory, few cores, data-saver or a slow link as weak",
      "isWeakClient > reads absent signals as capable (Firefox/Safari expose none)",
    ],
  },
  {
    file: "src/timeline.test.ts",
    tests: [
      "trackHeightVh > scales linearly with chapter count",
      "trackHeightVh > never collapses below one screen of travel",
      "chapterPosition > maps progress 0..1 across the full chapter range",
      "chapterPosition > clamps out-of-range progress",
      "chapterPosition > parks on the single chapter when count is 1",
      "resolveTimeline > starts in chapter 0 at localT 0",
      "resolveTimeline > ends in the last chapter",
      "resolveTimeline > reports index + fractional localT mid-chapter",
      "nearestChapter > rounds to the closest chapter for the HUD",
      "cardOpacity > is fully opaque at its own center",
      "cardOpacity > fades to zero beyond the falloff",
      "cardOpacity > is symmetric around the center",
      "cardOpacityWindowed > holds at FULL strength across the whole window",
      "cardOpacityWindowed > is gone just outside the eased edges",
      "cardOpacityWindowed > eases through the edges monotonically",
      "cardOpacityWindowed > honours asymmetric [in, out] eases (snap in, dissolve out)",
      "activeEra > reproduces nearest-chapter switch points with no overrides",
      "activeEra > honours an eraFrom override, holding the prior era until it",
      "activeEra > drives the real story eras — L-39 at 16.6 %, L-159 at 25.5 %, sunset at 58.7 %",
      "scroll weights (the climb + cruise + sunset stretches) > bakes the real story to total 13.3 (climb ×2, cruise ×1.6, sunset ×1.7)",
      "scroll weights (the climb + cruise + sunset stretches) > maps the knots of the weighted spans exactly",
      "scroll weights (the climb + cruise + sunset stretches) > posFromProgress inverts progressFromPos across the whole line",
      "scroll weights (the climb + cruise + sunset stretches) > is the identity when every weight is 1",
      "scroll weights (the climb + cruise + sunset stretches) > grows the track by exactly the extra weight (others keep their pace)",
      "chapter data integrity > has chapters to tell the story",
      "chapter data integrity > gives every chapter a unique id",
      "chapter data integrity > sets a sky variant exactly on sky chapters",
    ],
  },
]
